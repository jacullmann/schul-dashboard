FROM node:24
WORKDIR /app

# Install dependencies from package-lock.json for reproducible builds.
# Source is mounted via docker-compose volume — only node_modules live here.
COPY package*.json ./
RUN npm ci

CMD ["npm", "run", "dev"]
