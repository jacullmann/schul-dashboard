{
  "db_name": "PostgreSQL",
  "query": "UPDATE refresh_tokens\n                   SET revoked_at = now(), revoked_reason = $1\n                   WHERE id IN (\n                       SELECT id FROM refresh_tokens\n                       WHERE user_id = $2\n                         AND revoked_at IS NULL\n                         AND expires_at > now()\n                       ORDER BY issued_at ASC\n                       LIMIT GREATEST(0, (\n                           SELECT COUNT(*) FROM refresh_tokens\n                           WHERE user_id = $2\n                             AND revoked_at IS NULL\n                             AND expires_at > now()\n                       ) - ($3 - 1))\n                   )",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Text",
        "Uuid",
        "Int4"
      ]
    },
    "nullable": []
  },
  "hash": "d1530cf487606776e00bccfc4d324829d061a4adc79dbae6cf47eecd3a075716"
}
